# Context Injection (Telegram)

**Scope:** Telegram-native commands (e.g. `/context --add`, `/context --summary`) that enqueue semantic chunks into `main.semantic_memory` via Redis and the **DB-Writer**. Admin-only; aligns with Telegram Guard rules.

**Where to read the full spec:** canonical file in the repo:

`specs/features/Context Injection (Telegram).md`

**Related docs:** [Tri-Cameral Memory](../architecture/tri_cameral_memory.md) · [Installation — `/context` summary](../Installation.md) · [Commands (COMANDOS)](../COMANDOS.md) §2.3
