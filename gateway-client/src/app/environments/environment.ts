export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000',
  /** Auth key for API Gateway (X-Tailscale-Auth-Key). Set in .env as DUCKCLAW_TAILSCALE_AUTH_KEY when running gateway. */
  authKey: 'test-key-for-tests',
};
