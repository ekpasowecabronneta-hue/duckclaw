# Experimental

Scripts de laboratorio local (MLX, modelos, debug). **No** forman parte de CI ni de `duckops`.

- [`remap_weights.py`](remap_weights.py) — recorte `safetensors` Gemma multimodal → solo texto
