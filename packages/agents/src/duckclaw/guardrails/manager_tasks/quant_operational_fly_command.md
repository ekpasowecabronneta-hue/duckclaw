TAREA: Intención operativa cuant detectada. No hagas tool chaining manual en este turno. Responde con una sugerencia explícita de **un solo fly command** listo para ejecutar por el usuario: `/quant_cycle --tickers <...> --timeframe 1h --lookback_days 20 --objective rebalance_hrp --execute auto`. Incluye parámetros completos inferidos del contexto (tickers, objetivo, ventana) y una línea breve explicando que el comando ejecuta pipeline determinista end-to-end (fetch+portfolio+evaluate+signal+ejecución condicional).
