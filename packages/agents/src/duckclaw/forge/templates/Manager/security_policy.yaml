# Política Zero-Trust para sesiones efímeras "mercenario" del Manager.
network:
  default: deny
  allow_list: []
filesystem:
  readonly_mounts: []
  ephemeral_volumes:
    - /tmp/mercenary_workspace
secrets:
  in_memory_only: true
  allowed_secrets: []
max_execution_time_seconds: 300
